import './main.scss'

import Swiper from 'swiper/bundle';
import 'swiper/css/bundle';

const swiper = new Swiper('.swiper', {
    direction: 'horizontal',
    loop: true,
});

console.log("Hello! Swiper is ready.");

console.log("Hello This is a Typescript project.");

